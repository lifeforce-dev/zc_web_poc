# Local dev only
ZC_ENV=dev

# Frontend dev server origin (CORS + WebSocket origin check)
ALLOWED_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]

# In dev, frontend is cross-origin, so API must return absolute asset URLs.
ASSET_BASE_URL=http://localhost:8000/assets
